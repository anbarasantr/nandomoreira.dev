@import '../../stylus/index.styl'

.HeroText
  width 100%
  text-align center
  +above($tablet)
    height calc(100vh - 195px)
    display flex
    align-items center
    justify-content center
  &__inner
    padding 1.875rem .9375rem
    +above($tablet)
      padding .9375rem
